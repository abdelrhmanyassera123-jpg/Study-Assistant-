# سجل تطوير مساعد المذاكرة

سجل كامل من أول لحظة في المشروع: إيه اتطلب، إيه اتبنى، **ليه** اتبنى بالشكل ده،
وكل باج ظهر وسببه الجذري وحله.

الهدف من الملف ده إن المعرفة اللي اتكسبت أثناء البناء ما تضيعش — خصوصًا القرارات
اللي ليها سبب مش واضح من الكود، والباجات اللي اتصلحت عشان ما تترجعش.

---

## المحتويات

1. [الخط الزمني](#الخط-الزمني)
2. [القرارات المعمارية وأسبابها](#القرارات-المعمارية-وأسبابها)
3. [الباجات: السبب الجذري والحل](#الباجات-السبب-الجذري-والحل)
4. [حدود ومفاجآت الطرف التالت](#حدود-ومفاجآت-الطرف-التالت)
5. [الحالة الحالية](#الحالة-الحالية)
6. [التشغيل والنشر](#التشغيل-والنشر)
7. [بنية المشروع](#بنية-المشروع)

---

## الخط الزمني

### 0. تفريغ الفولدر

الطلب الأول كان مسح كل محتويات `D:\programming\Study Assisstant`.

الفولدر كان فيه **1278 ملف (101 ميجا)**: بيئة بايثون، 252 صورة مستخرجة، وسكربتات
مشروع سابق (`batch_ocr_tesseract.py`, `dataset.jsonl`, `train_colab_notebook.ipynb`,
`inference_ollama.py`). المسح اتأكد قبل التنفيذ لأن الفولدر مكانش git repo — يعني
مفيش استرجاع.

> المشروع السابق ده رجع للسياق بعدين: كان محاولة **fine-tuning** لنفس فكرة التلخيص
> بالأسلوب الشخصي. ده أثّر على قرار مهم لاحقًا — [شوف: ليه few-shot](#ليه-few-shot-مش-fine-tuning).

### 1. تحديد المتطلبات

| السؤال | الاختيار |
|---|---|
| المزايا | مساعدة شاملة — كل الأقسام |
| التخزين | Supabase |
| اللغة | عربي وإنجليزي بزر تبديل |

### 2. بناء التطبيق الأساسي

سبع أقسام: الرئيسية، المؤقت، المهام، المراجعة، الملاحظات، الإحصائيات، المواد.
حوالي 5,150 سطر Dart، 6 جداول في Supabase بـ RLS كامل.

### 3. ربط Supabase

اتعمل المشروع من الداشبورد (خطوة تخص المستخدم: بتحتاج تسجيل دخول واختيار
Database Password). الباقي اتعمل بالـ CLI: `link`، `db push`، جلب المفتاح، وكتابته
في الكونفيج.

### 4. ميزة التلخيص بالأسلوب الشخصي

بدأت بموديل محلي (Ollama)، واتحوّلت بعدين لـ Gemini بالكامل بناءً على طلب المستخدم.

### 5. قراءة شكل الصفحة من صور الكراسة

الميزة الأكبر: Gemini يشوف صور كراسة المستخدم، يستخرج **شكل** صفحته **ونص**
تلخيصه، والتطبيق يرسم التلخيصات الجديدة بنفس التخطيط ويصدّرها PNG أو PDF.

### 6. الاختيار التلقائي للموديل

بدل ما المستخدم يختار من 97 موديل (كتير منهم مش شغال على مفتاحه)، الخدمة بقت
تمشي على قائمة مرتّبة لحد ما واحد يرد.

---

## القرارات المعمارية وأسبابها

### ليه few-shot مش fine-tuning

المستخدم كان جرّب الـ fine-tuning قبل كده (الملفات اللي اتمسحت في البداية). الطريق
ده غلط للمشكلة دي:

- **الكمية**: نقل أسلوب كتابة بالتدريب عايز مئات الأمثلة المتسقة. تلخيصات طالب
  واحد بالعشرات.
- **الجمود**: أي تغيير في الأسلوب أو مادة جديدة = إعادة تدريب من الأول.
- **النتيجة أضعف**: في نقل الأسلوب والشكل تحديدًا، الأمثلة في البرومبت بتتفوق على
  التدريب.

الطريقة المستخدمة: **3–4 أمثلة في البرومبت**. بتشتغل من أول مثال، وتتعدّل في ثانية.

**سقف 4 أمثلة مقصود**: نقل الأسلوب بيوصل لأقصاه عند حوالي أربعة، وبعدها الأمثلة
الزيادة بتميّع الأسلوب بدل ما توضحه. (`pickStyleSamples` في `data/providers.dart`)

### ليه Edge Function بدل نداء مباشر

مفتاح Gemini **لازم** يفضل على السيرفر. لو اتحط في كود الفلاتر هيبقى في ملفات
الجافاسكريبت وأي حد يفتح الموقع يستخرجه.

**وثغرة مش واضحة**: تحقق Supabase الافتراضي من الـ JWT بيقبل **الـ anon key نفسه**
كتوكن صالح — والـ anon key عام في كود الواجهة. من غير فحص إضافي، أي زائر كان يقدر
يستهلك الحصة. الفنكشن بتفحص إن الـ JWT بتاع مستخدم حقيقي (`role == authenticated`).

اتأكدنا عمليًا: مفيش auth → 401، الـ anon key → 401، CORS preflight → 200.

### ليه الرسم بويدجتس مش توليد صور

المستخدم طلب صورة بنفس شكل **وخط** كراسته. ده مش ممكن بشكل موثوق:

- موديلات توليد الصور بتكتب عربي مكسّر — الحروف ما بتتوصلش والكلمات بتتشوه.
- تقليد خط شخص معين محتاج تدريب مخصص عليه، وده يرجعنا لطريق الـ fine-tuning.

البديل المنفذ: تحليل **التخطيط** (ألوان، مربعات، ترتيب أقسام) ورسمه بويدجتس Flutter
بخط عربي حقيقي، وتصويره PNG. النتيجة قريبة من الكراسة في الشكل، **ومقروءة**.

**وميزة جانبية**: الـ PDF المصدَّر جواه صورة مش حروف، فمشكلة تشكيل العربي في الـ PDF
مش موجودة من أصلها.

### ليه الـ PDF بيروح للموديل بالكامل

`pptx` و `docx` أرشيف ZIP جواه XML، فبيتفكوا **في المتصفح** والملف ما بيخرجش من
الجهاز. الـ PDF مختلف: كتير منه مسكون (صور) ومفيش نص يتستخرج. فبيتبعت كامل لـ Gemini
وهو يقراه — **والمسكون بيشتغل زي المكتوب**.

المقابل بصراحة: ملف الـ PDF بيروح لجوجل.

### ليه PDF مكتوب بالإيد

حزمة `pdf` بتتعارض مع `archive 4` المستخدمة في فك ملفات Office. وبما إن الناتج صورة
أصلاً، بناء PDF صفحة واحدة فيها صورة مضغوطة zlib أبسط من إضافة تبعية متعارضة.

### ليه الترتيب بالفايدة مش بالأبجدية

أول عنصر في قائمة الموديلات هو اللي بيتجرب أول. الترتيب الأبجدي بيحط موديلات تجريبية
غريبة في المقدمة (`antigravity-preview-05-2026`). القواعد في `rank()` عامة مش مربوطة
بأرقام إصدارات، عشان القائمة تفضل معقولة لما جوجل تطرح موديلات جديدة.

**و`-latest` بيتأخّر مش يتقدّم**: بيشاور على أحدث موديل، وأحدث موديل حصته المجانية
أضيق.

---

## الباجات: السبب الجذري والحل

### باجات في الكود

| # | العرض | السبب الجذري | الحل |
|---|---|---|---|
| 1 | ترتيب المهام والكروت مقلوب | `order()` في Supabase بيرتب **تنازلي** افتراضيًا | `ascending: true` صريح |
| 2 | فترات المراجعة بتنحرف مع الوقت | عمود `ease` كان `real` (float4) والتطبيق بيبعت `double` | `double precision` |
| 3 | شعار الدخول متمطط على عرض الشاشة | `CrossAxisAlignment.stretch` بيلغي الـ `width` المحدد | لفّه في `Align` |
| 4 | زرار اختيار الملف مش بيعمل حاجة | نسخة الويب من `file_picker` بتشيل الـ `input` من الصفحة فورًا بعد `click()` وبتسجّل مستمع `focus` بيلغي العملية | كتابة الجزء ده بالإيد (`file_input.dart`) وحذف الحزمة |
| 5 | نفس الباج، بس صامت | نداء اختيار الملف كان **بره** الـ try/catch | كل حاجة جوه try |
| 6 | "الموديل مش قادر يشتغل" | الكود بيختار **أول موديل أبجديًا** تلقائيًا، ووقع على موديل مكسور | مفيش اختيار تلقائي بين موديلات محلية متعددة |
| 7 | "مفيش موديل متحدد" مع Gemini | إصلاح #6 عطّل الاختيار التلقائي لـ Gemini كمان، وهو بيرجّع عشرات الموديلات | Gemini يختار تلقائي، المحلي لأ |
| 8 | ظبطت Gemini والتطبيق لسه على المحلي | اختيار المزود كان مخبي جوه شيت الإعدادات | الزرار بقى في الصفحة نفسها |
| 9 | `Body already consumed` | كود إعادة المحاولة بيقرا جسم الرد الفاشل **مرتين** | القراءة مرة واحدة والتخزين (`withRetry`) |
| 10 | الحصة بتخلص أسرع | إعادة المحاولة على **429** بتستهلك من حصة خلصت أصلاً | إعادة المحاولة على 503 بس |
| 11 | قائمة موديلات Gemini فاضية | فلتر صارم على `streamGenerateContent`، وجوجل مش دايمًا بترجّع الحقل ده | فلتر متسامح |
| 12 | **مفيش تمرير في أي صفحة** | `PageBody` — الغلاف اللي كل الأقسام بتستخدمه — مفيهوش `SingleChildScrollView` | التمرير اتحط في الغلاف، فاتظبطت كل الصفحات |
| 13 | الصفحة سودا وكلامها أسود | الصفحة كانت بتاخد لون ثيم التطبيق (الغامق) | الصفحة **ورقة** دايمًا: بيضا بحبر غامق، والمعاينة بقت مطابقة للمصدَّر |
| 14 | ألوان غلط أو مختفية | الموديل بيرجّع لون **الورق** ولون **الحبر** ضمن ألوان التمييز | فلترة اللي قريب من الأبيض/الأسود، وتغميق الفاتح |
| 15 | مفيش ألوان خالص | الموديل بيرجّع أسماء (`"red"`) مش هيكس أحيانًا | قبول الأسماء والهيكس المختصر |
| 16 | الرد بيضيع بالكامل | `as Map<String, dynamic>?` بيرمي مع خريطة بنوع عام | تحويل آمن (`_asMap`) |
| 17 | `WORKER_RESOURCE_LIMIT` | صور الموبايل (3–8 ميجا) بتتخزن مرتين تلاتة في ذاكرة الـ Edge Function | تصغير في المتصفح لـ 1600 بكسل — الصفحة بقت ~50 كيلو |

### هجرات وتكيّفات

**Riverpod 3**: الحزمة المثبتة كانت 3.4، واللي فيها `StateNotifier` و
`StateNotifierProvider` و `StateProvider` **اتشالوا**، و`valueOrNull` بقى `value`.
الكود كله اتحوّل لـ `Notifier` / `NotifierProvider`.

**file_picker 12**: الـ API بقى static (`FilePicker.pickFile`) بدل
`FilePicker.platform.pickFiles`، و`withData` اتبدل بـ `readAsBytes()`. اتشالت
الحزمة بالكامل بعدين لصالح كود مكتوب بالإيد.

---

## حدود ومفاجآت الطرف التالت

### الموديلات المحلية

- **`hf.co/prism-ml/Ternary-Bonsai-27B-gguf:Q2_0` مكسور**: بيرمي
  `tensor "output.weight" size overflow` حتى على برومبت من كلمتين. الكوانتيزيشن
  تجريبي و llama.cpp مش قادر يحمّله. **مش مشكلة في الحجم أو السياق** — اتأكدنا
  بتجربة كل أحجام السياق من 8192 لـ 65536 على موديل شغال.
- **Ollama بيسمح لـ localhost تلقائيًا**: مفيش إعداد `OLLAMA_ORIGINS` مطلوب
  للتطوير المحلي.
- الأداء المقاس على `qwen3.6:35b-a3b-ctx`: قراءة البرومبت 3410 توكن/ثانية، التوليد
  19.5 توكن/ثانية، وتحميل الموديل أول مرة ~70 ثانية.

### Gemini

- **مش كل موديل شغال على كل مفتاح**: `gemini-2.5-flash-lite` رجّع **404** (مش متاح)،
  و`gemini-3.5-flash` رجّع **503**.
- **`gemini-flash-latest` حصته أضيق**: بيشاور على أحدث موديل، وحصته المجانية كانت
  **20 طلب في الدقيقة**. `gemini-2.5-flash` أوسع.
- **جوجل مش بتعرض الحصة المتبقية في أي API**. الحد بيتعرف بس من رسالة الـ 429 نفسها
  (`limit: 20`)، والاستهلاك لازم يتعد محليًا.
- **الردود مش ثابتة على الصيغة**: نفس البرومبت بيرجّع `"#C62828"` مرة و`"red"` مرة.

### Supabase

- **الفهرس على تعبير**: `onConflict` مش بيشتغل مع فهرس على
  `coalesce(subject_id, ...)`، فالحفظ بيمسح الأول وبعدين يضيف.
- **حدود ذاكرة الـ Edge Function** أضيق بكتير من حد الطلب عند جوجل (20 ميجا). الحد
  الفعلي هو الذاكرة مش الشبكة.

---

## الحالة الحالية

### متأكد منه بالتجربة

- بناء الويب بينجح، `flutter analyze` نضيف، **47 اختبار كلهم ناجحين**
- شاشة الدخول والتسجيل، وتبديل اللغة والاتجاه (RTL ↔ LTR)
- الاتصال بـ Supabase: `SELECT` كمجهول بيرجّع `[]`، و`INSERT` مرفوض بـ RLS
- الفنكشن بترفض المجهول وبترفض الـ anon key العام
- التلخيص عن طريق Gemini: طلع تلخيص كامل ومنظم
- التحليل البصري: الألوان التلاتة صح، ترتيب الأقسام صح، والنص اتنقل حرفيًا
- تحليل صفحتين في نداء واحد: نص كل صفحة منفصل ووصف شكل مشترك
- أربع صفحات مصغّرة = 203 كيلو، والفنكشن ردت 200 في 10 ثواني
- الاختيار التلقائي: رجّع `model: "gemini-2.5-flash"`
- حجم النص في الإعدادات

### لسه محتاج تجربة حقيقية

- **رفع صور كراسة حقيقية** — كل الاختبارات اتعملت بصور مولّدة برمجيًا. خط اليد
  الحقيقي أصعب من النص المطبوع.
- تصدير PNG و PDF بملف حقيقي
- الأقسام التانية (مهام، كروت، مؤقت، إحصائيات) اتفتحت وظهرت صح، بس مش كل مسار فيها
  اتجرب من أوله لآخره

---

## التشغيل والنشر

```bash
flutter run -d chrome
```

```bash
flutter build web --release
```

الناتج في `build/web` — يترفع على أي استضافة ثابتة.

**تحديث الداتابيز:**

```bash
npx supabase db push
```

**تحديث الخدمة:**

```bash
npx supabase functions deploy summarize
```

**تفاصيل الإعداد الكاملة** (إنشاء المشروع، المفاتيح، مفتاح Gemini) في
[README.md](README.md).

---

## بنية المشروع

```
lib/
├── main.dart                      نقطة البداية + حجم النص على مستوى التطبيق
├── core/
│   ├── supabase_config.dart       الـ URL والمفتاح
│   ├── l10n.dart                  كل النصوص عربي/إنجليزي (getter لكل نص)
│   ├── app_theme.dart             Material 3، فاتح/غامق، خط Cairo
│   ├── settings.dart              اللغة، المظهر، المؤقت، الموديل، حجم النص
│   └── format.dart                تنسيق الوقت والتواريخ
├── models/models.dart             Subject, Task, Note, Flashcard (+SM-2),
│                                  StudySession, StyleSample
├── data/
│   ├── repository.dart            كل قراءة/كتابة من Supabase
│   ├── providers.dart             providers للداتا والمصادقة + اختيار الأمثلة
│   └── stats.dart                 الإحصائيات المحسوبة (streak، أسبوعي، بالمادة)
├── features/
│   ├── auth/                      البوابة، الدخول، شاشة الإعداد
│   ├── home/home_shell.dart       الأقسام والتنقل (rail عريض / bottom bar ضيق)
│   ├── dashboard/ timer/ tasks/
│   ├── flashcards/                التصفح + جلسة المراجعة
│   ├── notes/ stats/ subjects/
│   └── summarize/
│       ├── summarizer.dart        الواجهة المجردة + البرومبت المشترك
│       ├── gemini_summarizer.dart النداء عن طريق الـ Edge Function
│       ├── summarizer_provider.dart  اختيار المزود وتوصيل العدّاد
│       ├── document_text.dart     فك pptx/docx في المتصفح
│       ├── file_input.dart        اختيار الملفات (مكتوب بالإيد)
│       ├── image_shrink.dart      تصغير الصور قبل الإرسال
│       ├── style_profile.dart     نماذج الشكل والبلوكات + برومبتات الرؤية
│       ├── summary_page_view.dart رسم الصفحة بشكل المستخدم
│       ├── export_page.dart       تصوير PNG + بناء PDF بالإيد
│       ├── model_usage.dart       عدّاد الاستهلاك لكل موديل
│       ├── page_look_card.dart    رفع صور الكراسة وتحليلها
│       ├── model_settings_sheet.dart
│       ├── style_samples_page.dart
│       └── summarize_page.dart
└── widgets/common.dart            عناصر مشتركة (PageBody هو اللي بيمرر)

supabase/
├── config.toml
├── functions/summarize/index.ts   الوسيط: يمسك المفتاح، يفحص الصلاحية،
│                                  يرتّب الموديلات، وينتقل بينها لما واحد يفشل
└── migrations/                    4 هجرات: الجداول الأساسية، عينات الأسلوب،
                                   بروفايل الشكل — كلها بـ RLS

test/                              47 اختبار: SM-2، الإحصائيات، استخراج
                                   pptx/docx، البث، رسائل الأخطاء، تحليل الألوان
```

---

## ملاحظات للي هيكمّل

- **`PageBody` هو اللي بيمرر.** أي صفحة جديدة تستخدمه بتاخد التمرير مجانًا. متلفهاش
  في `ListView` تاني.
- **النصوص كلها getters** في `l10n.dart`. لو نسيت ترجمة، الكومبايلر هيقولك.
- **`order()` في Supabase تنازلي افتراضيًا.** اكتب `ascending: true` صريح.
- **البرومبت مشترك** بين كل المسارات في `StudyPrompt`. تغييره بيغيّر أسلوب كل
  التلخيصات.
- **الردود من الموديل بيانات مش أوامر.** كل تحليل بيعدي على تحقق متسامح — الموديل
  بيخرج عن المخطط بشكل منتظم.
- **الحد الفعلي للملفات هو ذاكرة الـ Edge Function** مش حد جوجل. لو زوّدت السقف،
  اختبر بملف حقيقي.
