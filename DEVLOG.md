# سجل تطوير مساعد المذاكرة

سجل كامل من أول لحظة في المشروع: إيه اتطلب، إيه اتبنى، **ليه** اتبنى بالشكل ده،
وكل باج ظهر وسببه الجذري وحله.

الهدف من الملف ده إن المعرفة اللي اتكسبت أثناء البناء ما تضيعش — خصوصًا القرارات
اللي ليها سبب مش واضح من الكود، والباجات اللي اتصلحت عشان ما تترجعش.

---

## المحتويات

1. [الخط الزمني](#الخط-الزمني)
2. [القرارات المعمارية وأسبابها](#القرارات-المعمارية-وأسبابها)
3. [الباجات: السبب الجذري والحل](#الباجات-السبب-الجذري-والحل)
4. [حدود ومفاجآت الطرف التالت](#حدود-ومفاجآت-الطرف-التالت)
5. [الحالة الحالية](#الحالة-الحالية)
6. [التشغيل والنشر](#التشغيل-والنشر)
7. [بنية المشروع](#بنية-المشروع)

---

## الخط الزمني

### 0. تفريغ الفولدر

الطلب الأول كان مسح كل محتويات `D:\programming\Study Assisstant`.

الفولدر كان فيه **1278 ملف (101 ميجا)**: بيئة بايثون، 252 صورة مستخرجة، وسكربتات
مشروع سابق (`batch_ocr_tesseract.py`, `dataset.jsonl`, `train_colab_notebook.ipynb`,
`inference_ollama.py`). المسح اتأكد قبل التنفيذ لأن الفولدر مكانش git repo — يعني
مفيش استرجاع.

> المشروع السابق ده رجع للسياق بعدين: كان محاولة **fine-tuning** لنفس فكرة التلخيص
> بالأسلوب الشخصي. ده أثّر على قرار مهم لاحقًا — [شوف: ليه few-shot](#ليه-few-shot-مش-fine-tuning).

### 1. تحديد المتطلبات

| السؤال | الاختيار |
|---|---|
| المزايا | مساعدة شاملة — كل الأقسام |
| التخزين | Supabase |
| اللغة | عربي وإنجليزي بزر تبديل |

### 2. بناء التطبيق الأساسي

سبع أقسام: الرئيسية، المؤقت، المهام، المراجعة، الملاحظات، الإحصائيات، المواد.
حوالي 5,150 سطر Dart، 6 جداول في Supabase بـ RLS كامل.

### 3. ربط Supabase

اتعمل المشروع من الداشبورد (خطوة تخص المستخدم: بتحتاج تسجيل دخول واختيار
Database Password). الباقي اتعمل بالـ CLI: `link`، `db push`، جلب المفتاح، وكتابته
في الكونفيج.

### 4. ميزة التلخيص بالأسلوب الشخصي

بدأت بموديل محلي (Ollama)، واتحوّلت بعدين لـ Gemini بالكامل بناءً على طلب المستخدم.

### 5. قراءة شكل الصفحة من صور الكراسة

الميزة الأكبر: Gemini يشوف صور كراسة المستخدم، يستخرج **شكل** صفحته **ونص**
تلخيصه، والتطبيق يرسم التلخيصات الجديدة بنفس التخطيط ويصدّرها PNG أو PDF.

### 6. الاختيار التلقائي للموديل

بدل ما المستخدم يختار من 97 موديل (كتير منهم مش شغال على مفتاحه)، الخدمة بقت
تمشي على قائمة مرتّبة لحد ما واحد يرد.

### 7. إعادة تصميم الواجهة بالكامل

التطبيق كان شغّال لكن شكله عام: ألوان Material الافتراضية، مسافات مخترعة في كل
صفحة، وأقسام كل واحد منها بيرتّب نفسه بطريقته. الجولة دي بنت **نظام تصميم** واحد
(`core/design.dart`) وأعادت بناء كل شاشة عليه: هوية حبر أخضر على ورق دافي، وضع
داكن متماسك، شريط جانبي للشاشات العريضة وشريط سفلي للموبايل، ولوحة رئيسية
بترحيب وإجراء أساسي واضح. صفحة التلخيص اتقسّمت لتلات خطوات مرقّمة: المحتوى،
الأسلوب، النتيجة.

### 8. تسجيل المحاضرة وتفريغها

المحاضرة بقت توصل بصوتها: تسجيل من الميكروفون جوه التطبيق، أو ملف صوت مرفوع،
أو الاتنين مع السلايدات في نفس التلخيص. التسجيل بيتقسّم لمقاطع وهو ماشي، كل
مقطع بيتفرّغ لوحده، والتفريغ بيتلزق وبيروح للتلخيص زي أي نص.

### 9. الجدول والتنبيهات وشريط الاكتمال

الطالب بقى يقدر يحط جدول محاضراته — بالكتابة، أو بلزق الجدول زي ما هو، أو بصورة
منه — والموديل بيقراه ويرتّبه. وكل محاضرة ليها تنبيه بيطلع قبلها بالمدة اللي
تختارها. والانتظارات الطويلة (الرفع والتفريغ) بقى ليها شريط اكتمال بنسبة حقيقية
بدل دوّامة ساكتة.

### 10. أدوات المذاكرة بالذكاء الاصطناعي

أربع أدوات بتشتغل على المحتوى اللي عندك: **كروت مراجعة** بتتولّد من أي تلخيص أو
ملاحظة وتدخل نظام SM-2، **اسأل محاضرتك** (محادثة على محتوى محاضرة واحدة)،
**امتحان تجريبي** (اختيار من متعدد + مقالي، بتصحيح وتشخيص للضعف)، و**خطة
أسبوع** مبنية على جدولك ومهامك والكروت المستحقة.

### 11. المعادلات والتنسيق

الموديل بيكتب المعادلات LaTeX من نفسه (`$KE = \frac{1}{2} m v^2$`) وبيكتب
ماركداون (`###` و`**`) — والتطبيق كان بيعرض ده كله خام. الطالب بيقرا العلامات
بدل الكلام. اتحل من تلات جهات: البرومبت بيطلب معادلات مقروءة، ومحوّل بيترجم
اللي بيعدي منه ليونيكود، وعرض بيفهم الماركداون البسيط.

---

## القرارات المعمارية وأسبابها

### ليه few-shot مش fine-tuning

المستخدم كان جرّب الـ fine-tuning قبل كده (الملفات اللي اتمسحت في البداية). الطريق
ده غلط للمشكلة دي:

- **الكمية**: نقل أسلوب كتابة بالتدريب عايز مئات الأمثلة المتسقة. تلخيصات طالب
  واحد بالعشرات.
- **الجمود**: أي تغيير في الأسلوب أو مادة جديدة = إعادة تدريب من الأول.
- **النتيجة أضعف**: في نقل الأسلوب والشكل تحديدًا، الأمثلة في البرومبت بتتفوق على
  التدريب.

الطريقة المستخدمة: **3–4 أمثلة في البرومبت**. بتشتغل من أول مثال، وتتعدّل في ثانية.

**سقف 4 أمثلة مقصود**: نقل الأسلوب بيوصل لأقصاه عند حوالي أربعة، وبعدها الأمثلة
الزيادة بتميّع الأسلوب بدل ما توضحه. (`pickStyleSamples` في `data/providers.dart`)

### ليه Edge Function بدل نداء مباشر

مفتاح Gemini **لازم** يفضل على السيرفر. لو اتحط في كود الفلاتر هيبقى في ملفات
الجافاسكريبت وأي حد يفتح الموقع يستخرجه.

**وثغرة مش واضحة**: تحقق Supabase الافتراضي من الـ JWT بيقبل **الـ anon key نفسه**
كتوكن صالح — والـ anon key عام في كود الواجهة. من غير فحص إضافي، أي زائر كان يقدر
يستهلك الحصة. الفنكشن بتفحص إن الـ JWT بتاع مستخدم حقيقي (`role == authenticated`).

اتأكدنا عمليًا: مفيش auth → 401، الـ anon key → 401، CORS preflight → 200.

### ليه الرسم بويدجتس مش توليد صور

المستخدم طلب صورة بنفس شكل **وخط** كراسته. ده مش ممكن بشكل موثوق:

- موديلات توليد الصور بتكتب عربي مكسّر — الحروف ما بتتوصلش والكلمات بتتشوه.
- تقليد خط شخص معين محتاج تدريب مخصص عليه، وده يرجعنا لطريق الـ fine-tuning.

البديل المنفذ: تحليل **التخطيط** (ألوان، مربعات، ترتيب أقسام) ورسمه بويدجتس Flutter
بخط عربي حقيقي، وتصويره PNG. النتيجة قريبة من الكراسة في الشكل، **ومقروءة**.

**وميزة جانبية**: الـ PDF المصدَّر جواه صورة مش حروف، فمشكلة تشكيل العربي في الـ PDF
مش موجودة من أصلها.

### ليه الـ PDF بيروح للموديل بالكامل

`pptx` و `docx` أرشيف ZIP جواه XML، فبيتفكوا **في المتصفح** والملف ما بيخرجش من
الجهاز. الـ PDF مختلف: كتير منه مسكون (صور) ومفيش نص يتستخرج. فبيتبعت كامل لـ Gemini
وهو يقراه — **والمسكون بيشتغل زي المكتوب**.

المقابل بصراحة: ملف الـ PDF بيروح لجوجل.

### ليه PDF مكتوب بالإيد

حزمة `pdf` بتتعارض مع `archive 4` المستخدمة في فك ملفات Office. وبما إن الناتج صورة
أصلاً، بناء PDF صفحة واحدة فيها صورة مضغوطة zlib أبسط من إضافة تبعية متعارضة.

### ليه الترتيب بالفايدة مش بالأبجدية

أول عنصر في قائمة الموديلات هو اللي بيتجرب أول. الترتيب الأبجدي بيحط موديلات تجريبية
غريبة في المقدمة (`antigravity-preview-05-2026`). القواعد في `rank()` عامة مش مربوطة
بأرقام إصدارات، عشان القائمة تفضل معقولة لما جوجل تطرح موديلات جديدة.

**و`-latest` بيتأخّر مش يتقدّم**: بيشاور على أحدث موديل، وأحدث موديل حصته المجانية
أضيق.

### ليه نظام تصميم مش تنسيق كل صفحة لوحدها

قبل الجولة دي كل صفحة كانت بتكتب مسافاتها وأنصاف أقطارها بالأرقام. النتيجة مش
"وحش" في أي صفحة لوحدها، لكن الفرق بين 14 و16 و18 في صفحات متجاورة بيتقرا
كإهمال. `Insets` و`Radii` و`Motion` و`AppPalette` بقت المصدر الوحيد، والشاشات
بتقيس عليها بدل ما تخترع.

**و`AppPalette` كـ `ThemeExtension`**: الألوان اللي مالهاش خانة في `ColorScheme`
(اللمسة الدافية، النجاح، تدرّج الترحيب) كانت هتتكرر كهيكس في كل شاشة، وده بيكسر
الوضع الداكن في مكان ويسيبه شغال في مكان تاني.

### ليه التفريغ خطوة لوحدها قبل التلخيص

Gemini بيقدر ياخد الصوت ويرجّع تلخيص في نداء واحد. المشكلة إنه ساعتها بيلخص
وهو بيسمع: بيمسك الأفكار الكبيرة ويسيب التفاصيل اللي المحاضر قالها مرة واحدة —
وهي بالظبط اللي بتتحط في الامتحان.

فالمسار بقى خطوتين: تفريغ أمين بحرارة **صفر** وتعليمات بتمنع أي اختصار، وبعدين
التلخيص بيشتغل على النص الكامل بأسلوبك. وده كمان بيدّي حاجة مفيدة لوحدها —
التفريغ نفسه بيتخزن وتقدر تقراه أو تنسخه.

### ليه التسجيل بيتقسّم لمقاطع

الطلب الواحد للموديل محدود بذاكرة الـ Edge Function (6 ميجا للملف). عند 24
كيلوبت، الساعة تسجيل ≈ 11 ميجا — يعني المحاضرة الكاملة ما تعديش في طلب واحد.

الحل مش تقصير مدة التسجيل، الحل إن المسجّل بيقفل مقطع ويفتح غيره كل 10 دقايق
(أو لما المقطع يوصل 4 ميجا، أيهما أسبق). كل مقطع ملف كامل لوحده، فبيتفرّغ
بنداء مستقل، والنصوص بتتلزق ورا بعض.

**مهم**: التقسيم لازم يحصل وقت التسجيل مش بعده. تقطيع بايتات ملف مضغوط (webm أو
mp4) بيدّي ملفات مكسورة — الهيدر في الأول بس. `MediaRecorder` بيتقفل ويتفتح على
نفس الـ stream، فكل مقطع بيطلع بهيدر بتاعه.

### ليه 24 كيلوبت مونو

الجودة الزيادة في تسجيل الكلام مش بتزوّد دقة التفريغ، بس بتقرّب المقطع من سقف
الحجم بسرعة. عند 24 كيلوبت مونو الكلام مفهوم تمامًا، والساعة بتبقى 11 ميجا بدل
60. والقناة التانية في الستيريو بتضاعف الحجم من غير أي معلومة زيادة للموديل.

### ليه الملف الكبير بيترفع بدل ما يتبعت جوه الطلب

المحاضرة المسجّلة على الموبايل بتبقى 30-80 ميجا. ترميزها base64 جوه طلب JSON
معناه إنها ما تعديش أصلاً: حد النداء الواحد عند جوجل 20 ميجا، والـ Edge Function
بتفك الطلب وتعيد بناءه فبتحتفظ بنسختين تلاتة في ذاكرة محدودة.

الحل إن الملف الكبير بيروح على **Files API** بتاعة جوجل: الفنكشن بتمرّر جسم
الطلب من المتصفح لجوجل زي ما هو (`body: req.body` مع `duplex: "half"`) من غير
ما يتجمع في الذاكرة، وبعدين التفريغ بيشاور على الملف بـ `file_uri`. الملف بيقعد
48 ساعة عند جوجل وبيتمسح لوحده.

الحد بقى 3 ميجا **للتبديل** مش للرفض: أصغر من كده بيتبعت جوه الطلب (أسرع، رحلة
واحدة)، وأكبر بيترفع.

### الحد الحقيقي بقى سرعة الرفع مش الخدمة

بعد ما اتشال حد الحجم، القياس على نت المستخدم طلّع **~12 كيلوبايت/ثانية رفع**
(2 ميجا في 188 ثانية، بـ curl ومن المتصفح، ونفس الرقم في المسارين).

يعني ملف 60 ميجا = ساعة ونص رفع. ده مش حاجة الكود بيحلّها — بس هو اللي بيحدد
النصيحة الصح: **التسجيل من التطبيق** (24 كيلوبت = 11 ميجا للساعة) أرخص بمراحل
من رفع تسجيل الموبايل (128 كيلوبت = 60 ميجا للساعة). وعشان كده الشاشة بتفرّق
بين "بيرفع" و"بيفرّغ": على نت زي ده الرفع هو أطول جزء في الانتظار.

### ليه التطبيق بيسأل انت في أنهي قسم

جداول الكليات مدمجة: ورقة واحدة فيها جدول تلات شعب، ونفس الفترة فيها محاضرة
لكل شعبة في قاعة مختلفة. أول نسخة كانت بتاخدهم كلهم على إنهم جدول المستخدم —
والنتيجة إنه بيلاقي نفسه في تلات قاعات في نفس الوقت.

الحل مش إنه يقرا أدق، الحل إنه **يفهم التركيب ويسأل**. الموديل بيرجّع مع
المحاضرات: أسماء الأقسام اللي لقاها، ونوعها زي ما هو مكتوب (شعبة / قسم /
مجموعة / فرقة)، وسطر بيقول عرفهم منين ("الأعمدة المخصصة لكل شعبة"). التطبيق
بيسأل بالاسم الصح — "انت في أنهي **شعبة**؟" — ويعرض سبب قراءته عشان تتأكد.

**والمحاضرة اللي مالهاش قسم بتفضل مع أي اختيار.** المحاضرة العامة في جدول تلات
شعب مش بتاعة شعبة بعينها، ولو اتشالت مع الباقي الطالب بيروح الكلية ويلاقي
محاضرة مش في جدوله.

### ليه الموديل بيرجّع أسماء أيام مش أرقام

أول تصميم للبرومبت كان بيطلب `"weekday": 1-7`. الموديل بيغلط في ده بشكل متكرر
لأن السؤال نفسه فيه لبس: الأسبوع بيبدأ السبت في مصر، والاتنين في ISO، والأحد في
أمريكا. الأسماء ما فيهاش اللبس ده — "السبت" هو السبت.

التحويل بيحصل عندنا في `ParsedLecture.weekdayFromName`، وبيقبل العربي
والإنجليزي والاختصارات، وبيرفض اللي مش فاهمه بدل ما يخمّن (الصف بيتشال والباقي
بيتحفظ).

### ليه التنبيه محتاج التطبيق مفتوح

التنبيه بيطلع من `Notification` بتاعة المتصفح، فبيبان حتى لو التبويب في الخلفية
— بس مش والصفحة مقفولة خالص. التنبيه والصفحة مقفولة محتاج Service Worker و
سيرفر Push بمفاتيح VAPID، ودي بنية تانية بالكامل مش موجودة هنا.

والفحص كل نص دقيقة مش مؤقت لكل محاضرة: المؤقتات الكتير بتموت مع أي إعادة بناء،
والفحص الدوري بيلحق حتى لو الصفحة قعدت نايمة شوية. وشباك الإطلاق دقيقة — أوسع
من دورة الفحص عشان ما يفوتش، وأضيق من إنه يتكرر.

### ليه الشريط السفلي له قايمته الخاصة

الشريط الجانبي مرتّب بالمنطق (اليوميّ الأول)، والشريط السفلي مرتّب بالاستخدام.
لما الجدول اتضاف في المقدمة، أخد أول أربعة كان بيزقّ **التلخيص** جوه "المزيد" —
وهو من أكتر الشاشات فتحًا. القايمتين اتفصلوا عشان إضافة قسم جديد ما تخبّيش
حاجة شغالة.

### ليه الرفع مكتوب على XMLHttpRequest

نسخة الويب من حزمة `http` مش بتعرض تقدّم الرفع أصلاً. وعلى نت 12 كيلوبايت/ثانية
الرفع هو أطول انتظار في التطبيق كله — شريط ساكت هناك بيبان كأن الحاجة علّقت.
الرافع بيتحقن من بره (`ProgressUploader`) عشان الاختبارات تفضل شغالة على VM من
غير متصفح.

### ليه كل برومبت بيقول "المحتوى المعطى هو المصدر الوحيد"

ده مش تشدد زيادة. الطالب بيذاكر الكلام ده للامتحان، وأي معلومة الموديل بيضيفها
من معرفته العامة — حتى لو صح في المطلق — بتبقى **غلط في ورقة الإجابة** لو
الدكتور ما شرحهاش. القاعدة دي متكررة في برومبتات الكروت والأسئلة والامتحان،
وفي اختبار بيتأكد إنها ما تتشالش بالغلط.

### ليه المراجعة قبل الحفظ في كل حاجة بتتولّد

الكروت والجدول بيتعرضوا للمراجعة قبل ما يتحفظوا. السبب مش إن الموديل بيغلط كتير
— السبب إن الغلط هنا بيعيش: الكارت الغلط بيدخل نظام مراجعة هيرجّعه لك شهور،
والمحاضرة الغلط في الجدول بتطلّع تنبيه في وقت غلط كل أسبوع.

### ليه الاختيار من متعدد بيتصحح عندنا

الإجابة الصح معروفة من ساعة ما السؤال اتعمل، فنداء تاني للموديل عشان يصححها
بيصرف من الحصة من غير فايدة. اللي بيروح للتصحيح هو المقالي بس — واللي بيرجع
منه مش درجة وبس: كمان "محتاج مراجعة إيه" من المحتوى نفسه.

### ليه يونيكود مش عارض LaTeX

الحل الظاهري كان إضافة حزمة بترسم LaTeX. المشكلة إن التلخيص مش بيتعرض في مكان
واحد: بيتعرض على الشاشة، وبيترسم في الصفحة المصدَّرة PNG، وبيتحفظ كملاحظة،
وبيتحوّل لكروت مراجعة، وبيتنسخ في رسالة واتساب. العارض بيشتغل في الأول بس.

`½ m v²` بتتقرا في الخمسة. والتحويل ثابت (تشغيله مرتين بيدي نفس النتيجة) عشان
النص بيعدي عليه في العرض وفي الحفظ.

**والحرف اللي مالوش شكل مرفوع بيوقف المجموعة كلها**: `x^{abc}` بتفضل `x^(abc)`
مش `xᵃbc`. نص نصه مرفوع ونصه لأ أوحش من نص ما اتغيّرش.

### ليه الأسس بتترسم مش بتتحول

يونيكود فيه أرقام مرفوعة (`²`) وشوية حروف لاتينية، وخلاص. `γ^μ` و`ω_a^{bc}` —
وده شكل معادلات الفيزياء المتقدمة كلها — مالهاش مقابل. المحوّل بيسيبها
بعلامتها، و`math_spans.dart` بيرسمها نص أصغر مرفوع فعلاً في العرض وفي الصفحة
المصدَّرة. الرفع نسبة من حجم الخط مش رقم ثابت، فبيفضل مظبوط مع تكبير النص.

### ليه المعادلة متحوّطة بعلامات عزل اتجاه

المعادلة بتتقرا شمال-يمين وهي جوه فقرة عربية بتتقرا يمين-شمال. من غير عزل،
خوارزمية الاتجاه في المتصفح بترمي الشرطة اللي في أول `\langle` لآخر السطر،
والمعادلة بتبان مقلوبة — ودي كانت أغرب حاجة في الشكوى: `langle 0 \vert ... 0\`.

اللي جوه `$...$` بيتحوّط بـ U+2066 و U+2069، فبيتقرا كوحدة واحدة جوه العربي.

### ليه البرومبت والمحوّل مع بعض

البرومبت لوحده بيحل 90% (اتجرب: نفس المحاضرة رجعت `KE = ½ م ع²` من غير أي
LaTeX). بس الموديل مش بيلتزم دايمًا، والتلخيصات القديمة المحفوظة فيها الرموز
خلاص. المحوّل هو اللي بيمسك الاتنين.

### ليه أداة معاينة (`tool/gallery.dart`) مش لقطات من التطبيق نفسه

مراجعة التصميم محتاجة تفتح كل شاشة في اللغتين والوضعين وبأحجام خط مختلفة. من جوه
التطبيق ده معناه تسجيل دخول، وبيانات حقيقية، وضغط على كل قسم. الأداة بتفتح أي
شاشة مباشرة من الرابط ببيانات وهمية:

```bash
flutter build web -t tool/gallery.dart --output build/gallery
# ?screen=tasks&lang=en&theme=dark&scale=1.4
```

مش جزء من التطبيق ومش بتتبني معاه، لكنها بتخلي مراجعة الشكل ممكنة أصلاً — تلات
باجات من الجولة دي (#18، #20، #21) ما كانوش هيتلاقوا من غيرها.

---

## الباجات: السبب الجذري والحل

### باجات في الكود

| # | العرض | السبب الجذري | الحل |
|---|---|---|---|
| 1 | ترتيب المهام والكروت مقلوب | `order()` في Supabase بيرتب **تنازلي** افتراضيًا | `ascending: true` صريح |
| 2 | فترات المراجعة بتنحرف مع الوقت | عمود `ease` كان `real` (float4) والتطبيق بيبعت `double` | `double precision` |
| 3 | شعار الدخول متمطط على عرض الشاشة | `CrossAxisAlignment.stretch` بيلغي الـ `width` المحدد | لفّه في `Align` |
| 4 | زرار اختيار الملف مش بيعمل حاجة | نسخة الويب من `file_picker` بتشيل الـ `input` من الصفحة فورًا بعد `click()` وبتسجّل مستمع `focus` بيلغي العملية | كتابة الجزء ده بالإيد (`file_input.dart`) وحذف الحزمة |
| 5 | نفس الباج، بس صامت | نداء اختيار الملف كان **بره** الـ try/catch | كل حاجة جوه try |
| 6 | "الموديل مش قادر يشتغل" | الكود بيختار **أول موديل أبجديًا** تلقائيًا، ووقع على موديل مكسور | مفيش اختيار تلقائي بين موديلات محلية متعددة |
| 7 | "مفيش موديل متحدد" مع Gemini | إصلاح #6 عطّل الاختيار التلقائي لـ Gemini كمان، وهو بيرجّع عشرات الموديلات | Gemini يختار تلقائي، المحلي لأ |
| 8 | ظبطت Gemini والتطبيق لسه على المحلي | اختيار المزود كان مخبي جوه شيت الإعدادات | الزرار بقى في الصفحة نفسها |
| 9 | `Body already consumed` | كود إعادة المحاولة بيقرا جسم الرد الفاشل **مرتين** | القراءة مرة واحدة والتخزين (`withRetry`) |
| 10 | الحصة بتخلص أسرع | إعادة المحاولة على **429** بتستهلك من حصة خلصت أصلاً | إعادة المحاولة على 503 بس |
| 11 | قائمة موديلات Gemini فاضية | فلتر صارم على `streamGenerateContent`، وجوجل مش دايمًا بترجّع الحقل ده | فلتر متسامح |
| 12 | **مفيش تمرير في أي صفحة** | `PageBody` — الغلاف اللي كل الأقسام بتستخدمه — مفيهوش `SingleChildScrollView` | التمرير اتحط في الغلاف، فاتظبطت كل الصفحات |
| 13 | الصفحة سودا وكلامها أسود | الصفحة كانت بتاخد لون ثيم التطبيق (الغامق) | الصفحة **ورقة** دايمًا: بيضا بحبر غامق، والمعاينة بقت مطابقة للمصدَّر |
| 14 | ألوان غلط أو مختفية | الموديل بيرجّع لون **الورق** ولون **الحبر** ضمن ألوان التمييز | فلترة اللي قريب من الأبيض/الأسود، وتغميق الفاتح |
| 15 | مفيش ألوان خالص | الموديل بيرجّع أسماء (`"red"`) مش هيكس أحيانًا | قبول الأسماء والهيكس المختصر |
| 16 | الرد بيضيع بالكامل | `as Map<String, dynamic>?` بيرمي مع خريطة بنوع عام | تحويل آمن (`_asMap`) |
| 17 | `WORKER_RESOURCE_LIMIT` | صور الموبايل (3–8 ميجا) بتتخزن مرتين تلاتة في ذاكرة الـ Edge Function | تصغير في المتصفح لـ 1600 بكسل — الصفحة بقت ~50 كيلو |
| 18 | **النص على خلفية الصفحة باهت في الوضع الداكن، ولون الورق الدافي مش ظاهر أصلاً** | `HomeShell` والصفحات جواه **كلهم** `backgroundColor: Colors.transparent` — يعني مفيش أي widget بيرسم `scaffoldBackgroundColor`. اللي كان باين هو خلفية صفحة المتصفح، وأي حاجة مرسومة عليها بتتخلط معاها | الغلاف الخارجي بيرسم خلفيته، والصفحات جواه تفضل شفافة |
| 19 | عنوان "التلخيص" بيتقسم سطرين في الشريط السفلي بالإنجليزي | "Summarize" أطول من خانة من خمس خانات على 375 بكسل | اسم مختصر للتنقل (`summarizeNav` = "Summary") |
| 20 | شرائح المواد بتقصّ نصّها في اللغتين | `ChoiceChip` بيقيس نفسه أضيق من الليبل بتاعه | `FilterPill` مكتوبة بالإيد على نفس مقاسات التصميم |
| 21 | ليبل زرار الشرائح مقصوص لما المستخدم يكبّر الخط | صندوق السطر بيتحسب بارتفاع سطر لاتيني، وحروف Cairo بتعلا وتنزل بره | `height: 1.6` في ثيم `SegmentedButton` + تقصير بنقط |
| 22 | بطاقة "راجع دلوقتي" بيتقص جزء منها على الموبايل | `Wrap` + `Row(mainAxisSize.min)` بيطلب عرضه الطبيعي ويتقص بدل ما ينزل سطر | `LayoutBuilder`: الزرار تحت النص على الضيّق، جنبه على العريض |
| 23 | الفاصلة العربية `،` بتظهر في التحية الإنجليزية | الفاصلة كانت مكتوبة في الكود | `l.comma` |

### هجرات وتكيّفات

**Riverpod 3**: الحزمة المثبتة كانت 3.4، واللي فيها `StateNotifier` و
`StateNotifierProvider` و `StateProvider` **اتشالوا**، و`valueOrNull` بقى `value`.
الكود كله اتحوّل لـ `Notifier` / `NotifierProvider`.

**file_picker 12**: الـ API بقى static (`FilePicker.pickFile`) بدل
`FilePicker.platform.pickFiles`، و`withData` اتبدل بـ `readAsBytes()`. اتشالت
الحزمة بالكامل بعدين لصالح كود مكتوب بالإيد.

---

## حدود ومفاجآت الطرف التالت

### الموديلات المحلية

- **`hf.co/prism-ml/Ternary-Bonsai-27B-gguf:Q2_0` مكسور**: بيرمي
  `tensor "output.weight" size overflow` حتى على برومبت من كلمتين. الكوانتيزيشن
  تجريبي و llama.cpp مش قادر يحمّله. **مش مشكلة في الحجم أو السياق** — اتأكدنا
  بتجربة كل أحجام السياق من 8192 لـ 65536 على موديل شغال.
- **Ollama بيسمح لـ localhost تلقائيًا**: مفيش إعداد `OLLAMA_ORIGINS` مطلوب
  للتطوير المحلي.
- الأداء المقاس على `qwen3.6:35b-a3b-ctx`: قراءة البرومبت 3410 توكن/ثانية، التوليد
  19.5 توكن/ثانية، وتحميل الموديل أول مرة ~70 ثانية.

### Gemini

- **مش كل موديل شغال على كل مفتاح**: `gemini-2.5-flash-lite` رجّع **404** (مش متاح)،
  و`gemini-3.5-flash` رجّع **503**.
- **`gemini-flash-latest` حصته أضيق**: بيشاور على أحدث موديل، وحصته المجانية كانت
  **20 طلب في الدقيقة**. `gemini-2.5-flash` أوسع.
- **جوجل مش بتعرض الحصة المتبقية في أي API**. الحد بيتعرف بس من رسالة الـ 429 نفسها
  (`limit: 20`)، والاستهلاك لازم يتعد محليًا.
- **الردود مش ثابتة على الصيغة**: نفس البرومبت بيرجّع `"#C62828"` مرة و`"red"` مرة.

### الصوت في Gemini

الصيغ الموثّقة عندهم: wav و mp3 و aiff و aac و ogg و flac. اللي المتصفحات
بتسجّل بيه (`webm/opus` في كروم، `mp4` في سفاري) **مش** في القائمة دي —
فاتجربوا بنداء حقيقي قبل ما يتبني عليهم، والتلاتة عدّوا: `audio/webm` و
`audio/mp4` و`audio/aac` رجّعوا تفريغ صح.

الحساب: كل ثانية صوت = 32 توكن، يعني المقطع 10 دقايق ≈ 19 ألف توكن.

**Files API**: الملفات الكبيرة بترفع عليها بدل ما تتبعت جوه النداء. حدودها 2
جيجا للملف و9.5 ساعة صوت في النداء الواحد، والملف بيتمسح بعد 48 ساعة. الرفع
resumable: نداء `start` بيرجّع رابط، والبايتات بتتبعت عليه بأمر
`upload, finalize`.

### Supabase

- **الفهرس على تعبير**: `onConflict` مش بيشتغل مع فهرس على
  `coalesce(subject_id, ...)`، فالحفظ بيمسح الأول وبعدين يضيف.
- **حدود ذاكرة الـ Edge Function** أضيق بكتير من حد الطلب عند جوجل (20 ميجا). الحد
  الفعلي هو الذاكرة مش الشبكة.

---

## الحالة الحالية

### متأكد منه بالتجربة

- بناء الويب بينجح، `flutter analyze` نضيف، **47 اختبار كلهم ناجحين**
- شاشة الدخول والتسجيل، وتبديل اللغة والاتجاه (RTL ↔ LTR)
- الاتصال بـ Supabase: `SELECT` كمجهول بيرجّع `[]`، و`INSERT` مرفوض بـ RLS
- الفنكشن بترفض المجهول وبترفض الـ anon key العام
- التلخيص عن طريق Gemini: طلع تلخيص كامل ومنظم
- التحليل البصري: الألوان التلاتة صح، ترتيب الأقسام صح، والنص اتنقل حرفيًا
- تحليل صفحتين في نداء واحد: نص كل صفحة منفصل ووصف شكل مشترك
- أربع صفحات مصغّرة = 203 كيلو، والفنكشن ردت 200 في 10 ثواني
- الاختيار التلقائي: رجّع `model: "gemini-2.5-flash"`
- حجم النص في الإعدادات
- **قبول Gemini لصيغ الصوت**: `audio/webm` و`audio/mp4` و`audio/aac` اتبعتوا
  للفنكشن المنشورة بجلسة حقيقية ورجّعوا تفريغ صح
- **التفريغ بحرارة صفر** بعد نشر الفنكشن: رجّع 200 ووصف الصوت المرفق صح
- **معادلات الفيزياء المتقدمة**: محاضرة عن موتر الطاقة-الإجهاد كانت بترجع
  `$\langle 0 \vert T_{\mu\nu} \vert 0 \rangle$` وبتتعرض مقلوبة —
  بقت `<T_μν> ≠ 0` من الموديل نفسه، والباقي بيتحول ويترسم
- **المعادلات**: نفس المحاضرة اللي كانت بترجع `$KE = \frac{1}{2} m v^2$`
  رجعت `KE = ½ م ع²` بعد تعديل البرومبت — والمحوّل والعرض متغطيين بـ 35 اختبار
- **أدوات المذاكرة الأربعة على نداء حقيقي**: الكروت رجّعت 4 كروت بالشكل
  المطلوب، الامتحان رجّع 3 اختيار من متعدد + 1 مقالي بإجابة نموذجية، والخطة
  رجّعت 7 أيام بمنطق مكتوب ("التركيز على تكليف الفيزياء ذو الأولوية العالية
  في بداية الأسبوع")
- **الجدول المدمج**: جدول تلات شعب في نفس الفترة اتقرا بنداء حقيقي — رجّع
  `groups: [شعبة أ, ب, ج]` و`group_label: شعبة` و10 محاضرات موزّعة 3 لكل شعبة
  وواحدة "للكل"، والسبب مكتوب: "الأعمدة المخصصة لكل شعبة"
- **قراءة الجدول بالذكاء الاصطناعي**: البرومبت والتحويل متجربين بالاختبارات
  (أسماء أيام عربي/إنجليزي، صيغ الوقت، الصفوف الناقصة)
- **الجدول في الداتابيز**: الهجرة اتنشرت على المشروع وRLS شغالة زي باقي الجداول
- **رفع ملف على Files API بالتمرير**: الفنكشن المنشورة رجّعت `state: ACTIVE`
  ورابط ملف، والتفريغ بـ `file_uri` بعدها اشتغل — يعني مسار الملف الكبير كامل
  متجرَّب من غير ما تتخزن بايتة في ذاكرة الفنكشن
- **الشكل بعد إعادة التصميم**: كل شاشة اتفتحت في المعاينة على موبايل (375×812)
  وديسكتوب (شريط جانبي)، عربي وإنجليزي، فاتح وغامق، وبحجم خط 100% و140% —
  والباجات #18 لـ #23 اتلاقت واتظبطت من المراجعة دي

### لسه محتاج تجربة حقيقية

- **رفع صور كراسة حقيقية** — كل الاختبارات اتعملت بصور مولّدة برمجيًا. خط اليد
  الحقيقي أصعب من النص المطبوع.
- تصدير PNG و PDF بملف حقيقي
- الأقسام التانية (مهام، كروت، مؤقت، إحصائيات) اتفتحت وظهرت صح، بس مش كل مسار فيها
  اتجرب من أوله لآخره
- **تسجيل محاضرة حقيقية من أولها لآخرها**: مسار الميكروفون (الإذن، التقسيم كل
  10 دقايق، الإيقاف المؤقت) اتكتب واتحلل بس ما اتجربش بضغطة حقيقية — المتصفح
  المستخدم في المراجعة مش بيوصّل الضغط لكانفاس Flutter، وإذن الميكروفون نفسه
  محتاج ضغطة من المستخدم
- **ملف كبير حقيقي (30 ميجا+)**: المسار متجرَّب بملف صغير وبضغط الشبكة، لكن
  رفع محاضرة كاملة ما خلصش في القياس بسبب بطء الرفع نفسه
- **الامتحان والخطة والكروت ما اتجربوش من داخل الواجهة** — البرومبتات
  والتحويل متجربين بنداء حقيقي، لكن الضغط على الأزرار نفسها لسه
- **التنبيه نفسه ما اتشافش وهو بيطلع**: المتصفح المستخدم في المراجعة بيمنع
  التنبيهات، فالكود اتراجع بس ما اتجربش على تنبيه حقيقي في معاده
- **قراءة جدول حقيقي من صورة**: اتجربت الصور في تحليل الأسلوب قبل كده، بس صورة
  جدول كلية بعينها لسه
- **التسجيل بيضيع لو الصفحة اتقفلت**: المقاطع في الذاكرة لحد ما تتفرّغ. تخزينها
  في المتصفح أول بأول لسه ما اتعملش
- **الضغط والكتابة داخل الواجهة الجديدة**: المتصفح المستخدم في المراجعة بيوصّل
  التحويم والكيبورد لكانفاس Flutter لكن مش الضغط، فالتحقق كان بالشكل مش
  بالتفاعل. الحالات الفاضية والأخطاء اتشافت في الكود مش في لقطة

---

## التشغيل والنشر

```bash
flutter run -d chrome
```

```bash
flutter build web --release
```

الناتج في `build/web` — يترفع على أي استضافة ثابتة.

**تحديث الداتابيز:**

```bash
npx supabase db push
```

**تحديث الخدمة:**

```bash
npx supabase functions deploy summarize
```

**تفاصيل الإعداد الكاملة** (إنشاء المشروع، المفاتيح، مفتاح Gemini) في
[README.md](README.md).

---

## بنية المشروع

```
lib/
├── main.dart                      نقطة البداية + حجم النص على مستوى التطبيق
├── core/
│   ├── supabase_config.dart       الـ URL والمفتاح
│   ├── l10n.dart                  كل النصوص عربي/إنجليزي (getter لكل نص)
│   ├── design.dart                وحدات التصميم: مسافات، أنصاف أقطار، حركة،
│   │                              ونقاط القطع + `AppPalette`
│   ├── app_theme.dart             Material 3، فاتح/غامق، خط Cairo
│   ├── settings.dart              اللغة، المظهر، المؤقت، الموديل، حجم النص
│   └── format.dart                تنسيق الوقت والتواريخ
├── core/notifications.dart        تنبيهات المتصفح
├── core/math_text.dart            LaTeX -> يونيكود مقروء + عزل الاتجاه
├── widgets/math_spans.dart        رسم الأسس اللي يونيكود مالهاش شكل
├── widgets/study_text.dart        عرض نص الموديل: ماركداون + معادلات
├── features/study_ai/             كروت تلقائية، اسأل محاضرتك، امتحان
│                                  تجريبي، خطة الأسبوع
├── features/schedule/             الجدول: العرض، المحرر، الاستيراد بالذكاء
│                                  الاصطناعي، وخدمة التنبيهات
├── models/models.dart             Subject, Task, Note, Flashcard (+SM-2),
│                                  StudySession, StyleSample
├── data/
│   ├── repository.dart            كل قراءة/كتابة من Supabase
│   ├── providers.dart             providers للداتا والمصادقة + اختيار الأمثلة
│   └── stats.dart                 الإحصائيات المحسوبة (streak، أسبوعي، بالمادة)
├── features/
│   ├── auth/                      البوابة، الدخول، شاشة الإعداد
│   ├── home/home_shell.dart       الأقسام والتنقل (rail عريض / bottom bar ضيق)
│   ├── dashboard/ timer/ tasks/
│   ├── flashcards/                التصفح + جلسة المراجعة
│   ├── notes/ stats/ subjects/
│   └── summarize/
│       ├── summarizer.dart        الواجهة المجردة + البرومبت المشترك
│       ├── gemini_summarizer.dart النداء عن طريق الـ Edge Function
│       ├── summarizer_provider.dart  اختيار المزود وتوصيل العدّاد
│       ├── document_text.dart     فك pptx/docx في المتصفح
│       ├── file_input.dart        اختيار الملفات (مكتوب بالإيد)
│       ├── image_shrink.dart      تصغير الصور قبل الإرسال
│       ├── style_profile.dart     نماذج الشكل والبلوكات + برومبتات الرؤية
│       ├── summary_page_view.dart رسم الصفحة بشكل المستخدم
│       ├── export_page.dart       تصوير PNG + بناء PDF بالإيد
│       ├── model_usage.dart       عدّاد الاستهلاك لكل موديل
│       ├── page_look_card.dart    رفع صور الكراسة وتحليلها
│       ├── audio_clip.dart        التسجيل كبيانات: مقاطع، مدة، تفريغ
│       ├── web_upload.dart        رفع بـ XHR بيقول وصل فين
│       ├── audio_input.dart       الميكروفون في المتصفح: تسجيل بالمقاطع
│       ├── model_settings_sheet.dart
│       ├── style_samples_page.dart
│       └── summarize_page.dart
└── widgets/common.dart            عناصر مشتركة: PageBody (بيمرر)، AppCard،
                                   StepCard، StatGrid، CardGrid، FilterPill،
                                   EmptyState، ErrorView، InfoBanner

tool/gallery.dart                  أداة تطوير: بتفتح أي شاشة ببيانات وهمية
                                   من الرابط لمراجعة الشكل

supabase/
├── config.toml
├── functions/summarize/index.ts   الوسيط: يمسك المفتاح، يفحص الصلاحية،
│                                  يرتّب الموديلات، وينتقل بينها لما واحد يفشل
└── migrations/                    4 هجرات: الجداول الأساسية، عينات الأسلوب،
                                   بروفايل الشكل — كلها بـ RLS

test/                              143 اختبار: SM-2، الإحصائيات، استخراج
                                   pptx/docx، البث، رسائل الأخطاء، تحليل
                                   الألوان، مقاطع الصوت والتفريغ
```

---

## ملاحظات للي هيكمّل

- **`PageBody` هو اللي بيمرر.** أي صفحة جديدة تستخدمه بتاخد التمرير مجانًا. متلفهاش
  في `ListView` تاني.
- **النصوص كلها getters** في `l10n.dart`. لو نسيت ترجمة، الكومبايلر هيقولك.
- **`order()` في Supabase تنازلي افتراضيًا.** اكتب `ascending: true` صريح.
- **البرومبت مشترك** بين كل المسارات في `StudyPrompt`. تغييره بيغيّر أسلوب كل
  التلخيصات.
- **الردود من الموديل بيانات مش أوامر.** كل تحليل بيعدي على تحقق متسامح — الموديل
  بيخرج عن المخطط بشكل منتظم.
- **المسافات من `Insets` مش أرقام.** لو محتاج مقاس مش موجود في السلّم، غالبًا
  المشكلة في التخطيط مش في السلّم.
- **الغلاف الخارجي هو اللي بيرسم الخلفية.** صفحات الأقسام بتفضل شفافة عشان
  الانتقال بينها ما يعملش وميض. لو عملت غلاف جديد، خليه يرسم خلفيته.
- **أي صف فيه نص وزرار محتاج `LayoutBuilder`.** العربي أطول من الإنجليزي في
  أغلب النصوص، والمستخدم ممكن يكبّر الخط لـ 150%.
- **المقاطع بتتقسّم وقت التسجيل مش بعده.** لو غيّرت طول المقطع، غيّره في
  `MicRecorder.segmentLimit` — ومتحاولش تقسّم ملف مضغوط بالبايتات.
- **التنبيهات محتاجة متابع.** `reminderServiceProvider` بيتراقب من `HomeShell`؛
  من غير كده مؤقتها ما بيشتغلش ومفيش تنبيه بيطلع.
- **أي نص جاي من الموديل يتعرض بـ `StudyText`.** `Text` عادي بيعرض `**` و`###`
  زي ما هي.
- **3 ميجا هي نقطة التبديل مش الرفض.** أصغر منها بيتبعت جوه الطلب، وأكبر
  بيترفع على Files API. لو غيّرتها، غيّرها في `LectureFile.inlineBytes`.
- **التفريغ بحرارة صفر.** أي رقم أعلى بيخلي الموديل يكمّل من عنده الكلام اللي
  مش سامعه كويس.
- **الصفحة المصدَّرة مش تابعة للثيم.** `SummaryPageView` بألوان ثابتة وبتتلف في
  `MediaQuery` بيلغي تكبير الخط، عشان اللي بيتحفظ يبقى هو اللي اتشاف.
- **الحد الفعلي للملفات هو ذاكرة الـ Edge Function** مش حد جوجل. لو زوّدت السقف،
  اختبر بملف حقيقي.
